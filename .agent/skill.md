## Build And Install

- After compiling the app, copy the built `Arpoon.app` bundle into `/Applications`.
- Use `./scripts/install-dev.sh` from the repo root to build with the repo-local derived data path and install `/Applications/Arpoon.app`.
